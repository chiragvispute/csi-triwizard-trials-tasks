🔹 1) Loops (for, while)

What: Used to iterate over elements of a list or sequence.

Example:

for x in [1, 0, 2, 0, 3]:
    print(x)

🔹 2) Conditional Statements (if)

What: Used to check conditions and control program flow.

Example:

x = 0
if x == 0:
    print("This is zero")

🔹 3) List Operations

What: Lists allow adding, removing, and checking elements.

Example:

arr = [1, 0, 2]
arr.append(3)
print(arr)  # [1, 0, 2, 3]

🔹 4) Indexing

What: Elements in a list can be accessed using their index (starting from 0).

Example:

arr = [1, 2, 3]
print(arr[0])  # 1