🔹 1. Loops (for, while)

You’ll iterate through the array to check numbers.


Example:

for x in [1, 0, 2, 0, 3]:
    print(x)

🔹 2. Conditional Statements (if)

Check if an element is 0 or not.

Example:

x = 0
if x == 0:
    print("This is zero")

🔹 3. List Operations

append() → to build a new array

remove() / insert() (optional, if you modify in place)

len() → get array size

Example:

arr = [1, 0, 2]
arr.append(3)
print(arr)  # [1, 0, 2, 3]

🔹 4. Indexing

You’ll need to access elements by index when rearranging.

Example:

arr = [1, 2, 3]
print(arr[0])  # 1

