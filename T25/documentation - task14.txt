🔹 1) Lists

What: Store candle heights in an array.

Example:

candles = [3, 2, 1, 3]
print(len(candles))  # 4

🔹 2) Built-in max()

What: Find the tallest candle height.

Example:

nums = [4, 9, 2]
print(max(nums))  # 9

🔹 3) Iteration Over Lists

What: Loop through candle heights to count how many match the maximum.

Example:

count = 0
for x in [3, 5, 5, 2]:
    if x == 5:
        count += 1
print(count)  # 2

🔹 4) Conditionals (if)

What: Compare each element with the tallest height.

Example:

n = 7
if n > 5:
    print("greater")

🔹 5) Input Conversion

What: Convert space-separated input into integers.

Example:

arr = list(map(int, input().split()))
print(arr)  # e.g. input: 1 2 3 → [1, 2, 3]